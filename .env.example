# if you don't want to set the value you can remove the line or comment it out with a hash (#)

PATH_URL=/
PATH_URL_IMG=public/upload/images/
PATH_URL_IMG_PRODUCT=public/upload/products/

DB_HOST=db_server
DB_PORT=3306
DB_USER=root
DB_PASS=root
DB_NAME=new_mvc_shop_db

# Primary SMTP Configuration (Gmail recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=tls
SMTP_UNAME=your-email@gmail.com
SMTP_PWORD=your-app-password

# Fallback SMTP Configuration (Mailtrap for testing)
SMTP_HOST_FALLBACK=smtp.mailtrap.io
SMTP_PORT_FALLBACK=2525
SMTP_UNAME_FALLBACK=your-mailtrap-username
SMTP_PWORD_FALLBACK=your-mailtrap-password

# Email Settings
FROM_EMAIL=noreply@sierrashop.com
FROM_NAME=Sierra Shop
REPLY_TO=support@sierrashop.com

################### For docker ###################
APP_NAME=nms

APP_PORT=85
SSL_PORT=445

MYSQL_PORT=13306
MYSQL_ROOT_PASS=root
MYSQL_DB=new_mvc_shop_db
MYSQL_USER=root
MYSQL_PASS=root

PHPMYADMIN_PORT=8183
PHPMYADMIN_UPLOAD_LIMIT=1024M
